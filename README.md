# ndice
CLI app for x86_64 Linux that rolls dice using pseudorandom number generation and gives outputs

## Instructions
Run `./ndice n m`, where `n` is the integer number of dice to roll, and `m` is the number of sides each has
